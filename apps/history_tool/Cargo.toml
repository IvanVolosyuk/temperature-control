[package]
name = "history_tool"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = { version = "4.0", features = ["derive"] }
anyhow = "1.0"
history = { path = "../../history" }
tokio = { version = "1", features = ["full"] }
chrono = "0.4"
